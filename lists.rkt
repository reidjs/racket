#lang racket
; the list of all planets in our solar system;
; the list of basic colors.

(displayln (cons 'Mercury (cons 'Venus (cons 'Earth (cons 'Mars (cons 'Jupiter (cons 'Saturn (cons 'Uranus (cons 'Neptune empty)))))))))

(displayln (cons 'Red (cons 'Green (cons 'Blue empty))))
